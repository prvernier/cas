
```r
print("hello")
```

```
## [1] "hello"
```

```r
x = bc10
dfSummary(x["nat_non_veg"], graph.col=FALSE)
```

```
## Error in dfSummary(x["nat_non_veg"], graph.col = FALSE): could not find function "dfSummary"
```

```r
dfSummary(x["non_for_anth"], graph.col=FALSE)
```

```
## Error in dfSummary(x["non_for_anth"], graph.col = FALSE): could not find function "dfSummary"
```

```r
dfSummary(x["non_for_veg"], graph.col=FALSE)
```

```
## Error in dfSummary(x["non_for_veg"], graph.col = FALSE): could not find function "dfSummary"
```
