P Vernier 2018-11-27

Windows notes:
Set the encoding to UTF-8 before copying
    > set client encoding to 'UTF-8';
Make sure your postgres user has access to this location
    - on Weasel, PostgreSQL can only read on E drive

conda install -c anaconda jupyter
conda install -c anaconda pandas
conda install folium -c conda-forge
conda install geopandas -c conda-forge
conda install -c conda-forge psycopg2

bc_vri.bat - Create spatial database using Anaconda Prompt

bc_vri.py

bc_vri.sql

bc_vri.R

bc_vri.shp 
